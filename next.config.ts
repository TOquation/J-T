import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  
  /* config options here */
  images: {
    domains: ['via.placeholder.com', 's3-alpha-sig.figma.com'], // Add all required domains here
  },
};

export default nextConfig;
